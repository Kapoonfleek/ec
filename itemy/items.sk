on rightclick:
    send "%player's tool%" to player
    if player's tool is (item amount of player's tool) of {sloikzexpemEN}:
        set {_int} to a random number between 1 and 50
        send "&8[&6Earthcraft&8] &7Otrzymujesz &6%{_int}% &7punktów doświadczenia!" to player