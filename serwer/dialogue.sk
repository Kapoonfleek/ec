

function dialogue(t: text, loc: location, wait: timespan, waitDelete: timespan, viewers: players = all players):
    wait {_wait}

    set {_maxLength} to 35
    set {_t} to "💬 %{_t}%"

    set {_bubbles} to rounded up (length of {_t}/{_maxLength})
    set {_waitNum} to 2 + (length of {_t})/10
    set {_waitTotal} to "%{_waitNum}% seconds" parsed as timespan
    set {_waitTotal} to {_waitTotal} + {_waitDelete}

    loop {_bubbles} times:
        set {_id} to random integer between 0 and 999999999999
        add {_id} to {_ids::*}

    set {_loops} to 5
    if {_bubbles} > 5:
        set {_loops} to {_loops} + {_bubbles}-5
        set {_extra} to 0.25*({_bubbles}-5)
    else:
        set {_extra} to -0.5 + (0.175*{_bubbles})

    set {_index} to 1
    set {_height} to -0.125*{_loops}
    loop {_ids::*}:
        set {_loc2} to location -2 + ({_height} + 0.25 * {_index}) under {_loc}
        set {_loc2} to location {_extra} above {_loc2}
        spawn fake armor stand at {_loc2} for {_viewers::*} with id loop-value
        

        set {_text} to subtext of {_t} from characters ({_maxLength}*{_index})-{_maxLength} to ({_maxLength}*{_index})-1
        set {_text} to unformatted "%{_text}%"
        
        set {_meta} to new metadata packet with id loop-value:
            name: {_text}
            name visible: true
            invisible: true
            stand marker: true

        send packet {_meta} to {_viewers::*}

        dialogueAnimate(loop-value, {_loops})
        chatBubbleRemove({_waitTotal}, loop-value)
        remove 1 from {_loops} if {_loops} > 1
        add 1 to {_index}

function chatBubble(p: player, t: text, viewers: players):
    set {_maxLength} to 30

    set {_bubbles} to rounded up (length of {_t}/{_maxLength})
    set {_waitNum} to 2 + (length of {_t})/10
    set {_wait} to "%{_waitNum}% seconds" parsed as timespan

    loop {_bubbles} times:
        set {_id} to random integer between 0 and 999999999999
        add {_id} to {_ids::*}

    if {chatbubbles::%{_p}%::*} is set:
        if {chatbubbles::%{_p}%::*} != {_ids::*}:
            loop {chatbubbles::%{_p}%::*}:
                remove fake entity with id loop-value for {_viewers::*}

    set {chatbubbles::%{_p}%::*} to {_ids::*}
    set {_loops} to 5
    if {_bubbles} > 5:
        set {_loops} to {_loops} + {_bubbles}-5
        set {_extra} to 0.25*({_bubbles}-5)
    else:
        set {_extra} to -0.5 + (0.175*{_bubbles})

    set {_index} to 1
    set {_height} to -0.125*{_loops}
    loop {_ids::*}:
        set {_loc} to location -2 + ({_height} + 0.25 * {_index}) under {_p}
        set {_loc} to location {_extra} above {_loc}
        spawn fake armor stand at {_loc} for {_viewers::*} with id loop-value

        set {_text} to subtext of {_t} from characters ({_maxLength}*{_index})-{_maxLength} to ({_maxLength}*{_index})-1
        set {_text} to unformatted "%{_text}%"
        
        set {_meta} to new metadata packet with id loop-value:
            name: {_text}
            name visible: true
            invisible: true
            stand marker: true

        send packet {_meta} to {_viewers::*}

        dialogueAnimate(loop-value, {_loops})
        chatBubbleRemove({_wait}, loop-value)
        remove 1 from {_loops} if {_loops} > 1
        add 1 to {_index}

function damageText(a: entity, e: entity, damage: number, crit: boolean, effective: string, modifier: number = 1.0):
    set {_source} to metadata value "LastDamageCause" of {_e}
    set {_id} to random integer between 0 and 999999999999
    set {_1} to a random integer between 1 and 2
    set {_2} to a random integer between 1 and 2

    if {_1} = 1:
        set {_x} to a random number between -1.0 and -0.5
    else:
        set {_x} to a random number between 1.0 and 0.5

    if {_2} = 1:
        set {_z} to a random number between -1.0 and -0.5
    else:
        set {_z} to a random number between 1.0 and 0.5

    set {_y} to a random number between 0.5 and 2
    
    set {_x} to (x-coordinate of {_e}) + {_x}
    set {_z} to (z-coordinate of {_e}) + {_z}
    set {_y} to (y-coordinate of {_e}) + {_y}

    set {_loc} to location({_x}, {_y}, {_z}, world "%{_e}'s world%")
    spawn fake text display entity at {_loc} for {_a} with id {_id}

    set {_damageFormatted} to formatNumberEc({_damage})
    set {_name} to "%{_damageFormatted}%"

    set {_maxHp} to max health of {_e}

    if any:
        {_maxHp} is not set
        {_maxHp} <= 0
    then:
        set {_maxHp} to 20

    set {_ratio} to {_damage} / {_maxHp}
    if {_ratio} > 1.0:
        set {_ratio} to 1.0
    set {_scale} to 0.75 + ({_ratio} * 1.5) + ({_damage} * 0.0001)
    set {_effectiveDamage::*} to "super strong", "strong", "moderately strong"
    set {_ineffectiveDamage::*} to "super weak", "weak", "mildly weak"

    if {_ineffectiveDamage::*} contains {_effective}:
        set {_scale} to {_scale}*0.67
        if {_effective} = "super weak":
            set {_name} to "&o%{_name}% ↓↓"
        else:
            set {_name} to "&o%{_name}% ↓"
        
    else if {_effectiveDamage::*} contains {_effective}:
        set {_scale} to {_scale}*1.1
        if {_effective} = "super strong":
            set {_name} to "%{_name}% ↑↑"
        else:
            set {_name} to "%{_name}% ↑"

    if {_crit} = true:
        set {_name} to "&l%{_name}%"
        set {_scale} to {_scale}*1.1
    
    if {_scale} > 5:
        set {_scale} to 5

    set {_meta} to new metadata packet with id {_id}:
        display text: "&c%{_name}%"
        display billboard: center
        display brightness block: 15
        display brightness sky: 15
        display seethrough: true
        display scale: vector({_scale}, {_scale}, {_scale})

    if {_effective} is set:
        if {_effective} != {_neutral}:
            set {_id2} to random integer between 0 and 999999999999
            spawn fake text display entity at location 0.25 above {_loc} for {_a} with id {_id2}
            set {_name} to "<##d5ff57>x%{_modifier}% (%{_effective}%)"
            set {_meta2} to new metadata packet with id {_id2}:
                display text: {_name}
                display billboard: center
                display brightness block: 15
                display brightness sky: 15
                display seethrough: true
                display scale: vector({_scale}*0.5, {_scale}*0.5, {_scale}*0.5)
            send packet {_meta2} to {_a}
    
    send packet {_meta} to {_a}

    set {_waitNum} to 2 + ({_damage}*0.02)
    set {_waitNum} to 5 if {_waitNum} > 5
    set {_wait} to "%{_waitNum}% seconds" parsed as timespan
    wait {_wait}
    remove fake entity with id {_id} for {_a}
    remove fake entity with id {_id2} for {_a}
    
function healText(a: entity, e: entity, heal: number, crit: boolean, modifier: number = 1.0):
    set {_id} to random integer between 0 and 999999999999
    set {_1} to a random integer between 1 and 2
    set {_2} to a random integer between 1 and 2

    if {_1} = 1:
        set {_x} to a random number between -1.0 and -0.5
    else:
        set {_x} to a random number between 1.0 and 0.5

    if {_2} = 1:
        set {_z} to a random number between -1.0 and -0.5
    else:
        set {_z} to a random number between 1.0 and 0.5

    set {_y} to a random number between 0.5 and 2
    
    set {_x} to (x-coordinate of {_e}) + {_x}
    set {_z} to (z-coordinate of {_e}) + {_z}
    set {_y} to (y-coordinate of {_e}) + {_y}

    set {_loc} to location({_x}, {_y}, {_z}, world "%{_e}'s world%")
    spawn fake text display entity at {_loc} for {_a} with id {_id}

    set {_healFormatted} to formatNumberEc({_heal})
    set {_name} to "%{_healFormatted}%"

    set {_maxHp} to max health of {_e}

    if any:
        {_maxHp} is not set
        {_maxHp} <= 0
    then:
        set {_maxHp} to 20
    set {_ratio} to {_heal} / {_maxHp}

    if {_ratio} > 1.0:
        set {_ratio} to 1.0
    set {_scale} to 0.75 + ({_ratio} * 1.5) + ({_heal} * 0.0001)

    if {_crit} = true:
        set {_name} to "&l%{_name}%"
        set {_scale} to {_scale}*1.1
    
    if {_scale} > 5:
        set {_scale} to 5

    set {_meta} to new metadata packet with id {_id}:
        display text: "&a%{_name}%"
        display billboard: center
        display brightness block: 15
        display brightness sky: 15
        display seethrough: true
        display scale: vector({_scale}, {_scale}, {_scale})

    set {_id2} to random integer between 0 and 999999999999
    spawn fake text display entity at location 0.25 above {_loc} for {_a} with id {_id2}
    set {_name} to "<#d5ff57>x%{_modifier}% effective"
    set {_meta2} to new metadata packet with id {_id2}:
        display text: {_name}
        display billboard: center
        display brightness block: 15
        display brightness sky: 15
        display seethrough: true
        display scale: vector({_scale}*0.5, {_scale}*0.5, {_scale}*0.5)
    
    send packet {_meta2} to {_a}
    send packet {_meta} to {_a}

    set {_waitNum} to 2 + ({_heal}*0.02)
    set {_waitNum} to 5 if {_waitNum} > 5
    set {_wait} to "%{_waitNum}% seconds" parsed as timespan
    wait {_wait}
    remove fake entity with id {_id} for {_a}
    remove fake entity with id {_id2} for {_a}

function shieldText(a: entity, e: entity, heal: number, crit: boolean, modifier: number = 1.0):
    set {_id} to random integer between 0 and 999999999999
    set {_1} to a random integer between 1 and 2
    set {_2} to a random integer between 1 and 2

    if {_1} = 1:
        set {_x} to a random number between -1.0 and -0.5
    else:
        set {_x} to a random number between 1.0 and 0.5

    if {_2} = 1:
        set {_z} to a random number between -1.0 and -0.5
    else:
        set {_z} to a random number between 1.0 and 0.5

    set {_y} to a random number between 0.5 and 2
    
    set {_x} to (x-coordinate of {_e}) + {_x}
    set {_z} to (z-coordinate of {_e}) + {_z}
    set {_y} to (y-coordinate of {_e}) + {_y}

    set {_loc} to location({_x}, {_y}, {_z}, world "%{_e}'s world%")
    spawn fake text display entity at {_loc} for {_a} with id {_id}

    set {_healFormatted} to formatNumberEc({_heal})
    set {_name} to "%{_healFormatted}%"

    set {_maxHp} to max health of {_e}
    
    if any:
        {_maxHp} is not set
        {_maxHp} <= 0
    then:
        set {_maxHp} to 20

    set {_ratio} to {_heal} / {_maxHp}
    if {_ratio} > 1.0:
        set {_ratio} to 1.0
    set {_scale} to 0.75 + ({_ratio} * 1.5) + ({_heal} * 0.0001)

    if {_crit} = true:
        set {_name} to "&l%{_name}%"
        set {_scale} to {_scale}*1.1
    
    if {_scale} > 5:
        set {_scale} to 5

    set {_meta} to new metadata packet with id {_id}:
        display text: "&b%{_name}%"
        display billboard: center
        display brightness block: 15
        display brightness sky: 15
        display seethrough: true
        display scale: vector({_scale}, {_scale}, {_scale})

    set {_id2} to random integer between 0 and 999999999999
    spawn fake text display entity at location 0.25 above {_loc} for {_a} with id {_id2}
    set {_name} to "<#d5ff57>x%{_modifier}% effective"
    set {_meta2} to new metadata packet with id {_id2}:
        display text: {_name}
        display billboard: center
        display brightness block: 15
        display brightness sky: 15
        display seethrough: true
        display scale: vector({_scale}*0.5, {_scale}*0.5, {_scale}*0.5)
    
    send packet {_meta2} to {_a}
    send packet {_meta} to {_a}

    set {_waitNum} to 2 + ({_heal}*0.02)
    set {_waitNum} to 5 if {_waitNum} > 5
    set {_wait} to "%{_waitNum}% seconds" parsed as timespan
    wait {_wait}
    remove fake entity with id {_id} for {_a}
    remove fake entity with id {_id2} for {_a}

function chatBubbleRemove(wait: timespan, id: integer):
    wait {_wait}
    remove fake entity with id {_id} for all players

import:
    com.destroystokyo.paper.profile.ProfileProperty
    org.bukkit.map.MinecraftFont

function dialogueAnimate(id: integer, loops: integer = 5):
    loop {_loops} times:
        move entity with id {_id} by 0 256 0 for all players
        wait 1 tick

function npcChat():
    #Format
    broadcast " &f'Brodaty' Zachary &709:58"
    broadcast " &8&l𜸩  &rThis is a test message to see%nl%       &8&l𜸩  &rthis is a test message again"


            