on any move:
	if {stun::%player%} is true:
		cancel event
on entity move:
	if {stun::%event-entity%} is true:
		cancel event
on damage:
	if damage cause is suffocation:
		victim is player:
			cancel event