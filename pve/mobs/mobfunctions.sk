function onECMobSpawn(mob: entity):
    set {_world} to "%world of {_mob}%"
    set {ECMobs::%{_world}%} to {ECMobs::%{_world}%} and (uuid of {_mob})

function onECMobDeath(mob: entity):
    set {_world} to "%world of {_mob}%"
    remove (uuid of {_mob}) from {ECMobs::%{_world}%}